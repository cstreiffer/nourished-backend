FROM node:13.12

# Copy the working directory
COPY ./ ./
RUN npm install
RUN npm install pm2 -g

# Set the node environment
ENV NODE_ENV=production

# Run it
EXPOSE 3001
CMD ["pm2", "start", "config/pm2.json"]