FROM node:13.12

# Copy the working directory
COPY ./ ./
RUN npm install
RUN npm install pm2 -g

# Store the JWTs
RUN bash scripts/generate-jwt-keys.sh

# Run it
EXPOSE 3001
CMD ["node", "server.js"]
