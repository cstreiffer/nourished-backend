FROM node:13.12

# Copy the working directory
COPY ./ ./
RUN npm install
RUN npm install pm2 -g

# Store the JWTs
RUN bash scripts/reload-jwt.sh

# 
COPY . .
EXPOSE 3001
CMD ["pm2", "start", "./config/pm2.json"]
